<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <root level="error">
        <appender-ref ref="APP_LOG"/>
    </root>

    <property name="DEV_HOME" value="./logs"/>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <appender name="APP_LOG" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator>
            <key>logFileName</key>
            <defaultValue>server_log</defaultValue>
        </discriminator>
        <sift>
            <appender name="FILE-${logFileName}" class="ch.qos.logback.core.rolling.RollingFileAppender">
                <file>${DEV_HOME}/${logFileName}.log</file>
                <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                    <pattern>%date{yy-MM-dd HH:mm:ss.SSS,GMT-5} %-5level [%F#%L] %msg\n</pattern>
                </encoder>
                <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
                    <FileNamePattern>${DEV_HOME}/${logFileName}.%i.log.zip
                    </FileNamePattern>
                    <MinIndex>1</MinIndex>
                    <MaxIndex>10</MaxIndex>
                </rollingPolicy>
                <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
                    <MaxFileSize>10MB</MaxFileSize>
                </triggeringPolicy>
            </appender>
        </sift>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>%date{yy-MM-dd HH:mm:ss.SSS,GMT-5} %-5level [%F#%L] %msg\n</pattern>
        </layout>
    </appender>

    <!-- SandboxServer -->
    <logger name="ninja.donhk.services.database.DatabaseServer" additivity="false">
        <level value="info"/>
        <appender-ref ref="APP_LOG"/>
    </logger>

    <!-- DBManager -->
    <logger name="ninja.donhk.services.database.DBManager" additivity="true">
        <level value="info"/>
        <appender-ref ref="APP_LOG"/>
    </logger>

    <!-- DatabaseServer -->
    <logger name="ninja.donhk.database.DatabaseServer" additivity="true">
        <level value="info"/>
        <appender-ref ref="APP_LOG"/>
    </logger>


</configuration>